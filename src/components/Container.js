import React from 'react'
import styled from "styled-components";

const Container = (main) => {
  return (
    <div>{main}</div>
  )
}

export default Container;
const StyledSDiv  = styled.div`
max-width: 1400px;

`
